export default function Sidebar() {
  return (
    <nav className="sidebar">
      <button>home</button>
      <button>projects</button>
      <button>info</button>
      <button>contact</button>
    </nav>
  );
}
